name: Precompile macOS ARM (Reusable)

on:
  workflow_call:
    inputs:
      package-name:
        description: Name of the Homebrew package to precompile
        type: string
        required: true
      expected-version:
        description: Expected package version (e.g. v1.2.3). Set to 'no check' to skip version validation.
        type: string
        default: 'no check'

jobs:
  precompile:
    runs-on: macos-latest
    env:
      PACKAGE_NAME: ${{ inputs.package-name }}
      EXPECTED_VERSION: ${{ inputs.expected-version }}
      ARCHITECTURE: arm
      WORKSPACE_DIR: ${{ github.workspace }}/workspace
    steps:
      - uses: actions/checkout@v4

      - name: Precompile package for macOS ARM
        run: bash scripts/precompile_for_macos.sh

      - name: Upload macOS ARM artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.package-name }}_macos_arm
          path: ${{ github.workspace }}/workspace/${{ inputs.package-name }}_macos_arm
